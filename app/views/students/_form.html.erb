<%= form_with(model: student) do |form| %>
  <% if student.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(student.errors.count, "error") %> prohibited this student from being saved:</h2>

      <ul>
        <% student.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label "Nome" %>
    <%= form.text_field :name %>
  </div>
  <br>         
  <div class="form-group">
    <div class="form-group col-md-2">
      <%= form.label "Selecione a Turma:" %>
      <%= form.collection_select :school_class_id, SchoolClass.all, :id, :name, class: "form-select" %>
    </div>
    <div class="form-group col-md-6">
      <%= form.label "Selecione o Responsável:" %>
      <%= form.collection_select :responsible_id, Responsible.all, :id, :name %>
    </div>
  </div>
  <br>        
  <div class="form-group">
    <label>Enviar imagem do aluno</label>
    <%= form.file_field :image, accept: 'image/png, image/gif, image/jpeg', class: "form-control-file" %>
  </div>

  <div>
    <%= form.submit "Criar aluno", class: "btn btn-primary" %>
  </div>
<% end %>
