<p style="color: green"><%= notice %></p>

<%= render @school_class %>

<div>
  <%= link_to "Edit this school class", edit_school_class_path(@school_class) %> |
  <%= link_to "Back to school classes", school_classes_path %>

  <%= button_to "Destroy this school class", @school_class, method: :delete %>
</div>

<h2>Registros de frequência</h2>
<%= render @school_class.frequency_registers %>

<%= link_to "Criar um Registro de Frequência", new_school_class_frequency_register_path(@school_class) %>

<p>Alunos da Turma:</p>
<%= render "school_classes/school_class_students_list" %>
